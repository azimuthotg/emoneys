# 🔒 สรุปการปรับปรุงสิทธิ์ Basic User

**วันที่:** 5 พฤศจิกายน 2568
**จุดประสงค์:** จำกัดสิทธิ์ Basic User ให้เห็นเฉพาะใบสำคัญของตัวเองเท่านั้น ไม่เห็นของคนอื่นในหน่วยงาน

---

## 📋 สาเหตุการแก้ไข

ผู้ดูแลระบบมหาวิทยาลัยแจ้งว่า **Basic User สามารถดูใบสำคัญรับเงินของคนอื่นในหน่วยงานเดียวกันได้** ซึ่งเป็นช่องโหว่ด้านความปลอดภัยและความเป็นส่วนตัว

---

## ✅ การแก้ไขที่ทำ

### 1. ไฟล์ที่แก้ไข

#### 📄 `accounts/views.py`

**1.1 receipt_list_view (บรรทัด 1480-1483)**
```python
# เปลี่ยนจาก
receipts = Receipt.objects.filter(department__name=request.user.get_department())
view_scope = f'หน่วยงาน: {request.user.get_department()}'

# เป็น
receipts = Receipt.objects.filter(created_by=request.user)
view_scope = 'ของฉัน'
```

**1.2 receipt_detail_view (บรรทัด 1582-1584)**
```python
# เปลี่ยนจาก
can_view = (receipt.department.name == request.user.get_department())

# เป็น
can_view = (receipt.created_by == request.user)
```

**1.3 reports_dashboard_view (บรรทัด 2660-2665)**
```python
# เพิ่มการเช็คสิทธิ์
if not (request.user.has_permission('report_view') or
        request.user.has_permission('receipt_view_department') or
        request.user.has_permission('receipt_view_all')):
    messages.error(request, 'คุณไม่มีสิทธิ์เข้าถึงหน้ารายงาน')
    return redirect('dashboard')
```

**1.4 receipt_report_view (บรรทัด 2872-2877)**
- เพิ่มการเช็คสิทธิ์เหมือน reports_dashboard_view

**1.5 revenue_summary_report_view (บรรทัด 3010-3015)**
- เพิ่มการเช็คสิทธิ์เหมือน reports_dashboard_view

---

#### 📄 `templates/base_sidebar.html`

**บรรทัด 332**
```html
<!-- เปลี่ยนจาก -->
{% if user.user_type == 'staff' %}

<!-- เป็น -->
{% if user.user_type == 'staff' and (user|has_perm:"report_view" or user|has_perm:"receipt_view_department" or user|has_perm:"receipt_view_all") %}
```

---

## 📊 ตารางสรุปการเปลี่ยนแปลง

| ฟีเจอร์ | ก่อนแก้ไข | หลังแก้ไข | Basic User | Dept Manager | Senior/Admin |
|---------|----------|-----------|-----------|--------------|--------------|
| **รายการใบสำคัญ** | ดูทั้งหน่วยงาน | ดูเฉพาะตัวเอง | ✅ เฉพาะตัวเอง | ✅ ทั้งหน่วยงาน | ✅ ทั้งหมด |
| **รายละเอียดใบสำคัญ** | ดูทั้งหน่วยงาน | ดูเฉพาะตัวเอง | ✅ เฉพาะตัวเอง | ✅ ทั้งหน่วยงาน | ✅ ทั้งหมด |
| **เมนูรายงาน** | มองเห็น | ซ่อน | ❌ ไม่เห็น | ✅ เห็น | ✅ เห็น |
| **เข้าหน้ารายงาน** | เข้าได้ | ห้ามเข้า (Redirect) | ❌ ห้าม | ✅ เข้าได้ | ✅ เข้าได้ |
| **ยกเลิกร่างตัวเอง** | ✅ ได้ | ✅ ได้ | ✅ ได้ | ✅ ได้ | ✅ ได้ |

---

## ✅ สิ่งที่ Basic User ยังทำได้ (ไม่เปลี่ยนแปลง)

- ✅ สร้างใบสำคัญใหม่
- ✅ ดูรายการใบสำคัญของตัวเอง
- ✅ แก้ไขร่างของตัวเอง
- ✅ **ยกเลิกร่างของตัวเองได้ทันที** (ไม่ต้องขออนุมัติ)
- ✅ เข้าดูรายละเอียดใบสำคัญของตัวเอง
- ✅ ส่งคำขอแก้ไข (สำหรับใบสำคัญเสร็จสิ้น)
- ✅ ส่งคำขอยกเลิก (สำหรับใบสำคัญเสร็จสิ้น)
- ✅ ดูคำขอแก้ไข/ยกเลิกของตัวเอง
- ✅ ถอนคำขอที่ส่งไปแล้ว

---

## 🔒 สิ่งที่ Basic User ทำไม่ได้แล้ว

- ❌ ดูใบสำคัญของคนอื่น (แม้หน่วยงานเดียวกัน)
- ❌ เข้าดูรายละเอียดใบสำคัญคนอื่น (Redirect + Error message)
- ❌ เห็นเมนูรายงาน
- ❌ เข้าหน้าแดชบอร์ดรายงาน (Redirect + Error message)
- ❌ เข้าหน้ารายงานใบสำคัญรับเงิน (Redirect + Error message)
- ❌ เข้าหน้ารายงานสรุปรายรับ (Redirect + Error message)

---

## 🧪 แผนการทดสอบ

### Test Case 1: รายการใบสำคัญ
**User:** Basic User
**Action:** เข้าหน้า `/accounts/receipt/list/`
**Expected:**
- ✅ เห็นเฉพาะใบสำคัญที่ตัวเองสร้าง
- ✅ ไม่เห็นใบสำคัญของคนอื่นแม้หน่วยงานเดียวกัน
- ✅ View scope แสดง "ของฉัน"

### Test Case 2: เข้าดูรายละเอียดใบสำคัญคนอื่น
**User:** Basic User
**Action:** พยายามเข้า URL `/accounts/receipt/<id>/` (ใบสำคัญของคนอื่น)
**Expected:**
- ✅ Redirect กลับหน้า `/accounts/receipt/list/`
- ✅ แสดง Error message: "คุณไม่มีสิทธิ์ดูใบสำคัญรับเงินนี้"

### Test Case 3: ยกเลิกร่างของตัวเอง
**User:** Basic User
**Action:** คลิกปุ่ม "ยกเลิก" ในร่างของตัวเอง
**Expected:**
- ✅ แสดง Modal ให้กรอกเหตุผล
- ✅ ยกเลิกได้ทันที (ไม่ต้องขออนุมัติ)
- ✅ เปลี่ยนสถานะเป็น "ยกเลิก"

### Test Case 4: เมนูรายงาน
**User:** Basic User
**Action:** เข้าระบบและดู Sidebar
**Expected:**
- ✅ ไม่เห็นเมนู "รายงาน"

### Test Case 5: เข้าหน้ารายงานโดยตรง (URL direct access)
**User:** Basic User
**Action:** พยายามเข้า URL `/accounts/reports/dashboard/`
**Expected:**
- ✅ Redirect กลับหน้า `/accounts/dashboard/`
- ✅ แสดง Error message: "คุณไม่มีสิทธิ์เข้าถึงหน้ารายงาน"

### Test Case 6: Department Manager ไม่กระทบ
**User:** Department Manager
**Action:** เข้าหน้า `/accounts/receipt/list/`
**Expected:**
- ✅ เห็นใบสำคัญทั้งหน่วยงาน
- ✅ View scope แสดง "หน่วยงาน: XXX"
- ✅ เห็นเมนูรายงาน
- ✅ เข้าหน้ารายงานได้

### Test Case 7: คำขอแก้ไข/ยกเลิก
**User:** Basic User
**Action:** เข้าหน้า `/accounts/edit-requests/` และ `/accounts/cancel-requests/`
**Expected:**
- ✅ เห็นเฉพาะคำขอของตัวเอง
- ✅ ไม่เห็นคำขอของคนอื่น

---

## 🎯 ผลลัพธ์ที่คาดหวัง

1. ✅ **Basic User เห็นเฉพาะข้อมูลของตัวเอง**
2. ✅ **ไม่สามารถเข้าถึงข้อมูลของคนอื่นได้** (แม้พยายามเข้า URL โดยตรง)
3. ✅ **Department Manager และ Senior Manager ไม่กระทบ**
4. ✅ **Basic User ยังยกเลิกร่างของตัวเองได้ตามปกติ**
5. ✅ **ระบบรายงานถูกจำกัดสิทธิ์เรียบร้อย**

---

## 📝 หมายเหตุ

- ไม่มีการแก้ไข Database Models หรือ Permissions
- การเปลี่ยนแปลงเป็นเพียง Business Logic ระดับ Views และ Templates
- Department Manager และ Senior Manager ทำงานได้ตามเดิมทุกประการ
- ระบบการอนุมัติคำขอแก้ไข/ยกเลิกไม่เปลี่ยนแปลง

---

## 🚀 Deployment Checklist

- [x] แก้ไข `accounts/views.py` (5 functions)
- [x] แก้ไข `templates/base_sidebar.html` (1 บรรทัด)
- [ ] Restart Django server
- [ ] ทดสอบ Test Cases ทั้ง 7 กรณี
- [ ] แจ้งผู้ใช้งานเกี่ยวกับการเปลี่ยนแปลง

---

**ผู้ดำเนินการ:** Claude Code
**วันที่อัพเดท:** 5 พฤศจิกายน 2568
